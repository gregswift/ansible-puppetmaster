---
- name: Place hourly cron job
  file: src=${this_playbook.dir}/puppetmaster.yml target=/etc/cron.hourly/ansible-puppetmaster owner=root group=root mode=0700

- name: Run the main playbook
  action: command /etc/cron.hourly/ansible-puppetmaster